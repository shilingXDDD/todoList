<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 精确闹钟：Android 12（API 31）起需要声明。
         Android 14（API 34）起该权限默认关闭，需要用户到
         设置 → 应用 → TodoList → 闹钟和提醒 手动开启。
         未授予时 ReminderManager 会自动退化为 setAndAllowWhileIdle（仍能触发，只是不那么准）。 -->
    <uses-permission
            android:name="android.permission.SCHEDULE_EXACT_ALARM"
            android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
            android:name=".TodoApplication"
            android:allowBackup="true"
            android:fullBackupContent="@xml/backup_rules"
            tools:targetApi="31"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Todolist">
        <activity android:name=".MainActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".ui.edit.TaskEditActivity"
                android:exported="false"
                android:parentActivityName=".MainActivity"/>

        <activity
                android:name=".ui.detail.TaskDetailActivity"
                android:exported="false"
                android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".ui.statistics.StatisticsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"/>


        <service
                android:name=".service.TaskSyncService"
                android:exported="false" />

        <receiver
                android:name=".receiver.TaskReceiver"
                android:exported="false" />


    </application>

</manifest>