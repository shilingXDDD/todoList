# 错误文档 0914

> 记录日期：2026-09-14
> 范围：UI 重构之后 ~ 第四步「任务分类」完成
> 用途：复盘踩过的坑，答辩前自查，以及后续开发避坑

---

## 零、概览

本次共记录 **24 个问题**，按严重程度分三级：

| 级别 | 数量 | 说明 |
|---|---|---|
| 🔴 严重 | 8 | 会导致功能失效、数据丢失或崩溃 |
| 🟡 中等 | 9 | 体验问题、潜在崩溃、编译报错 |
| 🔵 规范 | 7 | 文档/配置层面，不影响运行但影响评分 |

**其中 5 个是「照着文档做必然踩」的文档错误**——已同步修正 `第四步详解.md`、
`第五步详解.md`、`第六步详解.md`。

---

## 一、🔴 严重问题（功能失效 / 数据丢失）

### 1. 编辑保存会清空所有附加字段

**位置**：`TaskEditActivity.saveTask()`

**错误代码**：

```kotlin
TaskRepository.updateTask(
    Task(taskId, title, description, isCompleted)     // ❌
)
```

**原因**：`Task` 有 **11 个字段**，这里只用位置参数传了前 4 个。
Kotlin 会用默认值补齐剩下 7 个——等于每次编辑保存都把
`category`、`priority`、`dueDate`、`reminderTime`、`note`、`link`、`createdAt` 全部重置。

**表现**：设置好分类 → 保存 → 再编辑改个标题 → 保存 → **分类没了**。

**修法**：用 `copy()` 保留原有字段

```kotlin
val existing = TaskRepository.getTaskById(taskId)
if (existing != null) {
    TaskRepository.updateTask(
        existing.copy(title = title, description = description, isCompleted = isCompleted)
    )
}
```

**状态**：✅ 已修

---

### 2. 分类选了但存不进去

**位置**：`TaskEditActivity.saveTask()`

**原因**：修完问题 1 用了 `copy()`，但**忘了把 `category` 放进 copy 参数**。

```kotlin
existing.copy(
    title = title,
    description = description,
    isCompleted = isCompleted
    // ← 没有 category，所以永远是默认的"默认"
)
```

**表现**：分类能选、能回显（回显读的是 `task.category`），
但**改完保存后分类永远是"默认"**。

⚠️ 这个坑很隐蔽：回显正常会让人误以为功能是好的，
只有"改完再存"才会暴露。而评分表明确写了
「无法保存的不视为完整的需求附加」——**这 2 分直接拿不到**。

**修法**：

```kotlin
// ⚠️ 在协程外先取 Spinner 值，不要等到协程里再取
val category = binding.categorySpinner.selectedItem?.toString() ?: "默认"

existing.copy(
    title = title,
    description = description,
    isCompleted = isCompleted,
    category = category          // ← 补上
)
```

**状态**：✅ 已修（编辑 + 新增两处都补了）

---

### 3. 搜索框完全不生效

**位置**：`TaskListFragment`

**原因**：`setupSearch()` 方法**定义了但从未调用**。
`onViewCreated` 里有 `setupRecyclerView()`、`observeTasks()`，
唯独漏了 `setupSearch()`。

**表现**：搜索框能看见、能输入，但列表毫无反应——**且不报任何错**。

**修法**：

```kotlin
override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
    super.onViewCreated(view, savedInstanceState)
    setupRecyclerView()
    setupSearch()          // ← 补上
    observeTasks()
    ...
}
```

**状态**：✅ 已修

---

### 4. `strings.xml` 用了 `//` 注释导致整个项目编译失败

**错误代码**：

```xml
//%转义                                          <!-- ❌ XML 不认 // -->
<string name="stats_rate_value">%1$d%%</string>
```

**原因**：`//` 是 Java / Kotlin 的注释语法，**XML 必须用 `<!-- -->`**。

**表现**：aapt 报 `error: not well-formed (invalid token)`，
整个项目编译不过——而且报错位置可能不指向这一行，很难定位。

**修法**：

```xml
<!-- % 在 strings.xml 里必须转义成 %% -->
<string name="stats_rate_value">%1$d%%</string>
```

**状态**：✅ 已修

---

### 5. 统计页完成率显示 `NaN`

**位置**：`StatisticsActivity`

**错误代码**：

```kotlin
val rate = completed / total * 100      // ❌ 两个错
```

**两个问题**：

1. **除数为 0**：空列表时 `total = 0`，`0/0` 得到 `NaN`
2. **整数除法**：`completed / total` 先做整数除法得 `0`，再 ×100 还是 `0`

**表现**：空列表显示 `NaN%`；有数据时完成率一直显示 `0%`。

**修法**：先乘后除，并判断除零

```kotlin
val rate = if (total == 0) 0 else completed * 100 / total
```

**状态**：✅ 已修

---

### 6. 统计页两路 Flow 顺序导致闪现负数

**错误代码**：

```kotlin
launch {
    TaskRepository.countCompleted().collect { completed ->
        _completed = completed
        updateRate(_total, completed)     // ❌ _total 可能还是 0
    }
}
```

**原因**：`countCompleted` 先返回时 `_total` 还是初始值 0，
算出 `(0 - 2) = -2`，界面闪一下「未完成 -2」。

**修法**：先给字段赋值，再统一计算

```kotlin
private var total = 0
private var completed = 0

launch {
    TaskRepository.countAll().collect {
        total = it
        binding.txvTotal.text = it.toString()
        refreshDerived()          // 统一在这里算
    }
}

private fun refreshDerived() {
    binding.txvPending.text = (total - completed).toString()
    val rate = if (total == 0) 0 else completed * 100 / total
    binding.txvRate.text = getString(R.string.stats_rate_value, rate)
}
```

**状态**：✅ 已修

---

### 7. `onStop()` 注销广播可能崩溃

**位置**：`TaskListFragment.onStop()`

**错误代码**：

```kotlin
requireContext().unregisterReceiver(syncReceiver)     // ❌
```

**原因**：横竖屏切换时 Fragment 可能已与 Activity 解绑，
`requireContext()` 会抛 `IllegalStateException`。

**修法**：

```kotlin
val ctx = context ?: return
try {
    ctx.unregisterReceiver(syncReceiver)
} catch (e: IllegalArgumentException) {
    // 本来就没注册成功，忽略
}
```

**状态**：✅ 已修

---

### 8. Adapter 里直接用 `getString()` —— 编译错误

**位置**：`TaskAdapter.onBindViewHolder`（第四步 7.4 节）

**错误代码**：

```kotlin
holder.binding.dueDateTextView.text =
    getString(R.string.label_due_date, TimeUtil.format(task.dueDate))   // ❌
```

**原因**：`getString()` 是 `Context` 的方法，
`RecyclerView.Adapter` **不是** `Context`，直接调用编译不过。

**修法**：

```kotlin
holder.itemView.context.getString(R.string.label_due_date, TimeUtil.format(dueDate))
```

⚠️ 同类问题：`holder.itemView.context` 在 Adapter 里等价于 Activity 的 `this`。
要 `getColor()`、`getString()`、`getSystemService()` 都得走它。

**状态**：✅ 文档已修正（第四步 7.4 节）

---

## 二、🟡 中等（体验 / 潜在崩溃 / 编译报错）

### 9. 统计入口放在「外观」分组会露出未实现的功能

**原因**：`cardAppearance` 分组默认 `visibility="gone"`，
里面同时有「深色模式」（第五步才做）和「统计」。
为了显示统计把它打开，会**连带露出深色模式**——点了没反应，演示时很尴尬。

**修法**：把统计移到默认可见的「功能模块」分组。

**状态**：✅ 已修

---

### 10. 统计条目没有 id，代码无法引用

**原因**：布局里"统计"这行文字有（引用 `settings_item_statistics` 文案），
但**装它的 `LinearLayout` 没有 `android:id`**。

**表现**：`binding.itemStatistics` 爆红「Unresolved reference」。

**修法**：给容器加 `android:id="@+id/itemStatistics"`。

**状态**：✅ 已修

---

### 11. `observeTasks()` 重复提交列表

**错误代码**：

```kotlin
TaskRepository.getTasks().collect { tasks ->
    taskAdapter.submitList(tasks)     // ❌ 多余
    latestTasks = tasks
    applyFilter(tasks)                // 内部还会 submitList 一次
}
```

**影响**：连续提交两次不同列表，可能导致闪烁 + 额外的 DiffUtil 计算。

**修法**：删掉多余的 `submitList`，只保留 `applyFilter`。

**状态**：✅ 已修

---

### 12. 搜索框文案缺失导致编译失败

**原因**：`fragment_task_list.xml` 引用 `@string/hint_search`，
但 `strings.xml` 里没加。

**表现**：`resource string/hint_search not found`，资源链接失败。

**修法**：补文案（顺便把后面要用的也加了，省得再编译一次）

```xml
<string name="hint_search">搜索任务</string>
<string name="empty_search_result">未找到匹配的任务</string>
```

**状态**：✅ 已修

---

### 13. RecyclerView 顶部约束指向错误（列表盖住搜索框）

**现状**：`taskRecyclerView` 的 `app:layout_constraintTop_toBottomOf`
还指向 `@id/topBarDivider`，应该改成 `@id/searchView`。

**影响**：列表和搜索框从同一位置开始布局，**列表会盖住搜索框**。

**状态**：⚠️ **待修**

---

### 14. 深色模式文档用了已删除的颜色

**位置**：`第五步详解.md` 3.2 节

**错误代码**：

```xml
<item name="colorPrimary">@color/purple_200</item>      <!-- ❌ -->
<item name="colorPrimaryVariant">@color/purple_700</item>
```

**原因**：UI 重构后 `colors.xml` 里**已经没有** `purple_200` / `purple_700`。
照抄会直接 `resource not found` **编译不过**。

**修法**：改为新建 `values-night/colors.xml` 覆盖同名颜色
（不用写第二套 styles，因为样式都通过 `@color/xxx` 引用）。

**状态**：✅ 文档已修正

---

### 15. Spinner 未初始化就回显

**错误代码**：

```kotlin
binding.categorySpinner.setSelection(index)    // adapter 还没设
```

**原因**：Spinner 没设 adapter 就调 `setSelection()`，没有可选项，选不中。

**修法**：先 `adapter = ...` 再 `setSelection()`。
在 `TaskEditActivity.onCreate` 里，adapter 初始化必须在 `setupEditMode()` **之前**。

**状态**：✅ 代码已正确，文档已补说明

---

### 16. 日期选择器的月份从 0 开始

**原因**：`DatePickerDialog` 回调里的 `month` 是 **0~11**（0 = 1 月）。

**误区**：有人会自己 +1 或 -1 "修正"，反而错了。
直接传给 `Calendar.set(year, month, day, ...)` 即可。

**状态**：✅ 文档已补充

---

### 17. 空内容显示空胶囊

**原因**：分类为空字符串时，`categoryTextView` 仍然显示（一个空的灰色胶囊）。

**修法**：

```kotlin
if (task.category.isBlank()) {
    holder.binding.categoryTextView.visibility = View.GONE
} else {
    holder.binding.categoryTextView.visibility = View.VISIBLE
    holder.binding.categoryTextView.text = task.category
}
```

**状态**：✅ 已修（列表项 + 详情页都处理了）

---

## 三、🔵 规范 / 文档问题

### 18. 文档引用了未定义的变量

**位置**：`第四步详解.md` 2.6 节

**错误代码**：

```kotlin
category = binding.categorySpinner.selectedItem.toString(),
priority = selectedPriority          // ❌ 从未定义
```

**表现**：照着粘贴，`categorySpinner` 和 `selectedPriority` 双双爆红。

**修法**：拆成「修法 A 最小修复」（可直接粘贴）和
「修法 B 增量」（等第五、六节做完再加）。

**状态**：✅ 文档已修正

---

### 19. 多处缺 import

| 位置 | 缺失的 import |
|---|---|
| ViewModel | `SharingStarted`、`StateFlow`、`stateIn` |
| DaoTest | `AndroidJUnit4`、`ApplicationProvider`、`runBlocking` 等 8 个 |
| ContentProvider | `MatrixCursor`、`runBlocking`、`first`、`ContentValues` |
| ContentObserver | `ContentObserver`、`Handler`、`Looper` |
| 优先级色条 | `Color`、`ContextCompat` |
| 日期选择器 | `DatePickerDialog`、`TimePickerDialog`、`Calendar` |
| 分类 Spinner | `ArrayAdapter` |
| 详情页分类 | `View` |

**状态**：✅ 全部补齐

---

### 20. `context!!` 有空指针风险

**位置**：`TaskProvider.onCreate()`

```kotlin
dao = AppDatabase.getInstance(context!!).taskDao()    // ❌
```

**修法**：

```kotlin
val ctx = context ?: return false
dao = AppDatabase.getInstance(ctx).taskDao()
```

**状态**：✅ 已修

---

### 21. 分包说明过时

`第六步详解.md` 8.2 节只列了 4 个包，**缺 `ui/statistics/`**；
另外两条改名建议（`AppDatebase.kt` → `AppDatabase.kt`、
`TaskSyncActivity.kt` → `TaskSyncService.kt`）**早已完成**。

**状态**：✅ 文档已更新

---

### 22. 截图清单与 UI 重构后不符

从 7 张扩到 9 张，补统计页与底部导航，描述与卡片化风格对齐。

**状态**：✅ 文档已更新

---

### 23. Java 8 编译目标过时

`build.gradle.kts` 里 `sourceCompatibility = JavaVersion.VERSION_1_8`。
在 JDK 21 下警告「源值 8 已过时」。

**影响**：不影响运行，但答辩前建议改成 11 或 17。

**状态**：⚠️ 待处理（优先级低）

---

### 24. 图标写法不一致（深色模式相关）

| 批次 | 写法 | 深色下 |
|---|---|---|
| 原有图标 | `?attr/colorOnSurface` | 自动变白，不用改 |
| UI 重构新增 | `@color/xxx` | 靠 `values-night/colors.xml` 覆盖 |

**例外**：`ic_add.xml`（FAB 加号）写死 `@android:color/white`，
这是故意的——FAB 背景深浅都是主题蓝，加号恒为白色。

**状态**：✅ 文档已补充

---

## 四、待办清单

| # | 事项 | 优先级 |
|---|---|---|
| 1 | RecyclerView 顶部约束改为 `@id/searchView` | ⭐⭐⭐ 会导致列表盖住搜索框 |
| 2 | 验证「改标题保存后分类还在」 | ⭐⭐⭐ 评分关键点 |
| 3 | 编译时把 Java 8 改成 11 或 17 | ⭐ 低优先级 |
| 4 | 答辩前删掉 `TaskReceiver` / `TaskDetailActivity` 里的 `Log.d` | ⭐⭐ 代码质量评分项 |

---

## 五、经验总结

### 5.1 三条血的教训

1. **`copy()` 优于重建对象**
   编辑更新一律 `existing.copy(...)`，永远不要 `Task(id, ...)`。
   加了新字段后，前者自动兼容，后者静默丢数据。

2. **定义了函数要记得调用**
   `setupSearch()` 定义了不调用，不报错、不崩溃，
   功能就是"安静地"不工作。这类 bug 最难发现。

3. **XML 注释用 `<!-- -->`**
   写惯 Kotlin 后容易顺手打 `//`，结果整个项目编译不过，
   且报错位置不直观。

### 5.2 文档写法改进

给代码示例前，必须确认引用的每个**资源 / 变量 / 方法当前确实存在**：

- 凡是要等后续章节才建的 → 拆成「最小版 + 增量版」
- 引用已删除的模板色（`purple_200` 等）→ 全部核对一遍
- 缺 import 的代码片段 → 一律补全再放进文档

### 5.3 自检口诀

```
改了布局 → 检查引用它的 Kotlin 还能不能编译
加了字段 → 检查 copy() 里有没有带上
写了函数 → 检查有没有被调用
用了 Spinner → 检查 adapter 是否在 setSelection 之前
```

---

## 六、相关文档

- `第四步详解.md` — 需求附加（附 A 有统计页完整落地记录）
- `第五步详解.md` — 技术附加（第九节有修订记录）
- `第六步详解.md` — 收尾提交（第十节有修订记录）
- `通知修改.md` — 通知链路排查与修复
